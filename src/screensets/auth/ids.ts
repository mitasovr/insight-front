/**
 * auth Screenset IDs
 *
 * ALL unique identifiers for this screenset in one place.
 * Public screens — no auth required.
 */

export const AUTH_SCREENSET_ID = 'auth';

export const CALLBACK_SCREEN_ID = 'callback';
