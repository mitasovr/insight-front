/**
 * auth API Mocks
 * Mock data for development and testing
 *
 * Keys use full URL patterns (including baseURL path).
 * Register mocks via MockPlugin in main.tsx or screenset.
 */

import type { MockMap } from '@hai3/react';

/**
 * Mock map for auth API service
 * Add your mock endpoints here
 *
 * Example:
 * 'GET /api/auth/items': () => mockItems,
 * 'POST /api/auth/items': (data) => ({ id: Date.now(), ...data }),
 */
export const authMockMap = {
  // Add your mocks here
} satisfies MockMap;
