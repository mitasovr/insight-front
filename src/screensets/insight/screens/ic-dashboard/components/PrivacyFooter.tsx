/**
 * PrivacyFooter — privacy notice at the bottom of the IC dashboard.
 * No props, no state imports.
 */

import React from 'react';

const PrivacyFooter: React.FC = () => (
  <div className="text-center text-[10px] text-gray-400 pt-2">
    This is your personal dashboard. Your manager sees a summary — not the full detail shown here.
  </div>
);

export default PrivacyFooter;
